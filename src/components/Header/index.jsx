import React, {Component, Fragment} from 'react';
import {Button} from 'evergreen-ui';

import './style.scss';

const Header = (props) => {
  return (
    <div >
    </div>
  );
};

export default Header;
